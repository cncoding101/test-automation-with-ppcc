const urls = {
  base: 'https://playwright.dev',
  olfstaging: 'https://frontend.staging.onlinefuels.io',
};

enum urlTypes {
  base = 'base',
  olfstaging = 'olfstaging',
}

export default { urls, urlTypes };
